<UserControl x:Class="Diffusion.Toolkit.Controls.ComfyNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Diffusion.Toolkit.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Border Background="Transparent" BorderThickness="1" BorderBrush="{StaticResource SecondaryBrush}" Margin="5">
            <Grid Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Label Content="{Binding Name}" Background="#80000000"></Label>
                <StackPanel Background="Transparent" x:Name="InputsPanel" Grid.Row="1">
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
