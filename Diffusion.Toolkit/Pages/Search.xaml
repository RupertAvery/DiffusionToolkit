<Page x:Class="Diffusion.Toolkit.Pages.Search"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Diffusion.Toolkit.Pages"
      xmlns:models="clr-namespace:Diffusion.Toolkit.Models"
      xmlns:toolkit="clr-namespace:Diffusion.Toolkit"
      xmlns:classes="clr-namespace:Diffusion.Toolkit.Classes"
      xmlns:controls="clr-namespace:Diffusion.Toolkit.Controls"
      xmlns:converters="clr-namespace:Diffusion.Toolkit.Converters"
      xmlns:fa="http://schemas.fontawesome.io/icons/"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance models:SearchModel, IsDesignTimeCreatable=True}"
      d:DesignHeight="615" d:DesignWidth="800"
      Title="Search">
    <Page.Resources>
        <ResourceDictionary>

            <Storyboard x:Key="gridLoading">
                <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" Storyboard.TargetName="Page">
                    <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                    <EasingDoubleKeyFrame KeyTime="0:0:1" Value="1">
                        <EasingDoubleKeyFrame.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </EasingDoubleKeyFrame.EasingFunction>
                    </EasingDoubleKeyFrame>
                </DoubleAnimationUsingKeyFrames>
                <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" Storyboard.TargetName="Page">
                    <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                    <EasingDoubleKeyFrame KeyTime="0:0:1" Value="1"/>
                </DoubleAnimationUsingKeyFrames>
            </Storyboard>
            <!--<Style BasedOn="{StaticResource {x:Type Button}}"
                   TargetType="Button"
                   x:Key="BorderlessButton">
                <Setter Property="BorderBrush" Value="Transparent"/>
            </Style>
            <Style BasedOn="{StaticResource {x:Type Label}}"
                   TargetType="Label"
                   x:Key="TitleText">
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="DemiBold"/>
                <Setter Property="Foreground" Value="DarkGray">
                </Setter>
            </Style>
            <Style x:Key="Preview" TargetType="Grid">
                <Setter Property="Background" Value="DarkGray"></Setter>
            </Style>-->



            <converters:InverseBoolToVisibilityCollapsedConverter x:Key="invBoolToVisCol"></converters:InverseBoolToVisibilityCollapsedConverter>
            <converters:BoolToVisibilityCollapsedConverter x:Key="boolToVisCol"></converters:BoolToVisibilityCollapsedConverter>
            <converters:InverseBoolToVisibilityConverter x:Key="invBoolToVis"></converters:InverseBoolToVisibilityConverter>
            <converters:IntToVisibilityConverter x:Key="intToVis"></converters:IntToVisibilityConverter>
            <converters:BoolToBlurIntConverter x:Key="boolToBlur"></converters:BoolToBlurIntConverter>
            <converters:BoolToBlurIntMultiConverter x:Key="boolToBlurMulti"></converters:BoolToBlurIntMultiConverter>
            <converters:BoolToVisibilityConverter x:Key="boolToVis"></converters:BoolToVisibilityConverter>
            <converters:BoolToVisibilityMultiConverter x:Key="boolToVisMulti"></converters:BoolToVisibilityMultiConverter>
            <converters:InverseBoolToVisibilityMultiConverter x:Key="invBoolToVisMulti"></converters:InverseBoolToVisibilityMultiConverter>
            <converters:BoolToOpacityConverter x:Key="BoolToOpacity"></converters:BoolToOpacityConverter>
        </ResourceDictionary>
        <!--<DataTemplate DataType="{x:Type toolkit:ImageEntry}">
            <StackPanel Orientation="Vertical" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                <Image Source="{Binding Thumbnail}" Height="128" Width="128" MaxWidth="128" MaxHeight="128" HorizontalAlignment="Stretch" VerticalAlignment="Top" Stretch="UniformToFill" />
                <TextBlock Text="{Binding FileName}" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" MaxWidth="128"/>
            </StackPanel>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Path=(toolkit:MyScrollViewer.IsInViewport), RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}"
                         Value="True">
                    <Setter  TargetName="Thumbnail" Property="Source" Value="{Binding Thumbnail}" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>-->
    </Page.Resources>
    <Grid x:Name="MainGrid" Background="{DynamicResource {x:Static SystemColors.ControlBrush}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>


        <Grid  Grid.Row="0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="38"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Label FontSize="24" FontWeight="DemiBold" Content="{Binding ModeName}"></Label>
            </Grid>
            <Grid Grid.Row="1" Margin="2">
                <ComboBox x:Name="SearchTermTextBox" 
                          Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                          ItemsSource="{Binding SearchHistory}"
                          PreviewKeyDown="SearchTermTextBox_OnPreviewKeyDown"
                          SelectionChanged="SearchTermTextBox_OnSelectionChanged"
                          IsEditable="True"
                          Padding="5,0,0,0"
                          HorizontalAlignment="Stretch" 
                          VerticalContentAlignment="Center">
                    <ComboBox.InputBindings>
                        <KeyBinding Command="{Binding HideDropDown}" Key="Escape" />
                        <KeyBinding Command="{Binding SearchCommand}" Key="Return" CommandParameter="ManualSearch" />
                    </ComboBox.InputBindings>
                </ComboBox>
                <TextBlock IsHitTestVisible="False" 
                           Text="{Binding SearchHint}" FontStyle="Italic" 
                           VerticalAlignment="Center" 
                           HorizontalAlignment="Left" 
                           Padding="8,0,0,0" Foreground="DarkGray">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchTermTextBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
            <Label Grid.Row="2" Visibility="{Binding IsEmpty, Converter={StaticResource boolToVis}}" 
                   Style="{StaticResource TitleText}"
                   Content="{Binding ResultStatus}"
                   VerticalContentAlignment="Center" 
                   HorizontalContentAlignment="Center" 
            ></Label>
            <ListView Grid.Row="2" x:Name="ThumbnailListView" Margin="2" Padding="10" ItemsSource="{Binding Images, NotifyOnTargetUpdated=True}"
                      SelectionMode="Extended"
                      SelectedItem="{Binding SelectedImageEntry}" 
                      KeyDown="ThumbnailListView_OnKeyDown"
                      PreviewMouseLeftButtonDown="ThumbnailListView_OnPreviewMouseLeftButtonDown"
                      MouseDown="ThumbnailListView_OnMouseDown"
                      MouseMove="ThumbnailListView_OnMouseMove"
                      MouseDoubleClick="Control_OnMouseDoubleClick"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      Tag="{Binding Path=.}"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                <ListView.Resources>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Name="Border" Padding="2" SnapsToDevicePixels="true">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="true">
                                            <Setter TargetName="Border" Property="Background"
                                                    Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.Resources>
                <ListView.InputBindings>
                    <KeyBinding Command="{Binding PrevPage}" Key="PageUp" Modifiers="Alt"  Gesture="ALT+PageUp"></KeyBinding>
                    <KeyBinding Command="{Binding NextPage}"  Key="PageDown" Modifiers="Alt" Gesture="ALT+PageDown"></KeyBinding>
                    <KeyBinding Command="{Binding FirstPage}"  Key="Home" Modifiers="Alt" Gesture="ALT+Home"></KeyBinding>
                    <KeyBinding Command="{Binding LastPage}"  Key="End" Modifiers="Alt" Gesture="ALT+End"></KeyBinding>
                    <KeyBinding Command="{Binding CopyFiles}" Key="C" Modifiers="Control"  Gesture="Control+C"></KeyBinding>
                    <KeyBinding Command="{Binding Refresh}" Key="F5" Modifiers="Control"  Gesture="Control+F5"></KeyBinding>
                    <KeyBinding Command="{Binding FocusSearch}" Key="F6" Gesture="F6"></KeyBinding>
                    <KeyBinding Command="{Binding ToggleParameters}" Key="I"></KeyBinding>
                </ListView.InputBindings>
                <ListView.Template>
                    <ControlTemplate >
                        <controls:MyScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Background="Transparent">
                            <WrapPanel IsItemsHost="True" />
                        </controls:MyScrollViewer>
                    </ControlTemplate>
                </ListView.Template>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" Background="Transparent" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate  DataType="{x:Type toolkit:ImageEntry}">
                        <Border CornerRadius="5" BorderBrush="LightGray" BorderThickness="2" Height="128" Width="128" Margin="12" >
                            <Grid >
                                <controls:Thumbnail 
                                    DataContext="{Binding .}"
                                    Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType={x:Type ListView}}}"
                                    Source="{Binding Thumbnail, IsAsync = True}" 
                                    Opacity="{Binding Path=ForDeletion, Converter={StaticResource BoolToOpacity}}"
                                    Height="128" Width="128" 
                                    MaxWidth="128" MaxHeight="128" 
                                    HorizontalAlignment="Stretch" VerticalAlignment="Top">

                                    <controls:Thumbnail.ContextMenu>
                                        <ContextMenu DataContext="{Binding Path=PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                            <MenuItem Header="Favorite (F)" ToolTip="F" InputGestureText="F" Command="{Binding FavoriteCommand}"/>
                                            <MenuItem Header="Rate">
                                                <MenuItem Header="1" Command="{Binding RatingCommand}" CommandParameter="1"></MenuItem>
                                                <MenuItem Header="2" Command="{Binding RatingCommand}" CommandParameter="2"></MenuItem>
                                                <MenuItem Header="3" Command="{Binding RatingCommand}" CommandParameter="3"></MenuItem>
                                                <MenuItem Header="4" Command="{Binding RatingCommand}" CommandParameter="4"></MenuItem>
                                                <MenuItem Header="5" Command="{Binding RatingCommand}" CommandParameter="5"></MenuItem>
                                                <MenuItem Header="6" Command="{Binding RatingCommand}" CommandParameter="6"></MenuItem>
                                                <MenuItem Header="7" Command="{Binding RatingCommand}" CommandParameter="7"></MenuItem>
                                                <MenuItem Header="8" Command="{Binding RatingCommand}" CommandParameter="8"></MenuItem>
                                                <MenuItem Header="9" Command="{Binding RatingCommand}" CommandParameter="9"></MenuItem>
                                                <MenuItem Header="10" Command="{Binding RatingCommand}" CommandParameter="10"></MenuItem>
                                                <Separator></Separator>
                                                <MenuItem Header="Remove rating" Click="Unrate_OnClick"></MenuItem>
                                            </MenuItem>
                                            <Separator />
                                            <MenuItem Header="Copy prompt"  Command="{Binding CopyPromptCommand}"/>
                                            <MenuItem Header="Copy negative prompt"  Command="{Binding CopyNegativePromptCommand}"/>
                                            <MenuItem Header="Copy seed"  Command="{Binding CopySeedCommand}"/>
                                            <MenuItem Header="Copy hash"  Command="{Binding CopyHashCommand}"/>
                                            <MenuItem Header="Copy all parameters"  Command="{Binding CopyParametersCommand}"/>
                                            <Separator />
                                            <MenuItem Header="Show in explorer"  Command="{Binding ShowInExplorerCommand}"/>
                                            <!--<Separator />
                                            <MenuItem Header="Add to Album" />
                                            <MenuItem Header="Remove from Album" />-->
                                            <Separator />
                                            <MenuItem Visibility="{Binding CurrentImage.ForDeletion, Converter={StaticResource invBoolToVisCol}}" Header="Send to Trash (Del)" ToolTip="Del" Command="{Binding DeleteCommand}"/>
                                            <MenuItem Visibility="{Binding CurrentImage.ForDeletion, Converter={StaticResource boolToVisCol}}" Header="Restore (Del)" ToolTip="Del" Command="{Binding DeleteCommand}"/>
                                            <!--<MenuItem Header="Delete immediately (Shift+Del)" />-->
                                        </ContextMenu>
                                    </controls:Thumbnail.ContextMenu>
                                    <controls:Thumbnail.Effect>
                                        <BlurEffect  KernelType="Gaussian">
                                            <BlurEffect.Radius>
                                                <MultiBinding Converter="{StaticResource boolToBlurMulti}" ConverterParameter="20">
                                                    <Binding Path="NSFW"></Binding>
                                                    <Binding ElementName="MainGrid" Path="DataContext.NSFWBlur" ></Binding>
                                                </MultiBinding>
                                            </BlurEffect.Radius>
                                        </BlurEffect>
                                    </controls:Thumbnail.Effect>
                                </controls:Thumbnail>
                                <!--Margin="96,96,0,0"-->
                                <StackPanel Margin="0,96,0,0" Orientation="Horizontal">
                                    <Image IsHitTestVisible="False" Height="24" Width="24" Source="../Icons/blue-heart-32.png" Visibility="{Binding Favorite, Converter={StaticResource boolToVisCol}}">
                                        <Image.Effect>
                                            <DropShadowEffect BlurRadius="3" Opacity="0.8"  ShadowDepth="4" Direction="-90" RenderingBias="Quality" />
                                        </Image.Effect>
                                    </Image>
                                    <Grid Background="Transparent">
                                        <Image Visibility="{Binding Rating, Converter={StaticResource intToVis}, ConverterParameter=1}" IsHitTestVisible="False" Height="24" Width="24" Source="../Icons/star-32.png">
                                            <Image.Effect>
                                                <DropShadowEffect BlurRadius="3" Opacity="0.8"  ShadowDepth="4" Direction="-90" RenderingBias="Quality" />
                                            </Image.Effect>
                                        </Image>
                                        <!--<Label Foreground="White"  FontWeight="Bold" IsHitTestVisible="False" Height="24" Width="24"  Content="{Binding Rating}" HorizontalAlignment="Center" HorizontalContentAlignment="Center">
                                            <Label.Effect>
                                                <DropShadowEffect BlurRadius="2" Opacity="0.8"  ShadowDepth="1" Direction="-90" RenderingBias="Quality" />
                                            </Label.Effect>
                                        </Label>-->
                                    </Grid>
                                </StackPanel>
                                <!--<Image IsHitTestVisible="False" Height="32" Width="32" Source="../Icons/x-mark-32.png" Visibility="{Binding ForDeletion, Converter={StaticResource boolToVis}}"/>-->
                            </Grid>
                        </Border>
                        <!--<DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Path=(toolkit:MyScrollViewer.IsInViewport), RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}}"
                                         Value="True">
                                <Setter TargetName="Thumbnail" Property="Source" Value="{Binding Thumbnail}" />
                            </DataTrigger>
                        </DataTemplate.Triggers>-->
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0"  
                         VerticalAlignment="Stretch" 
                         HorizontalAlignment="Stretch" 
                         VerticalContentAlignment="Center"
                         Content="{Binding Results}"
                ></Label>
                <StackPanel Grid.Column="1"   Orientation="Horizontal">
                    <Button Style="{DynamicResource BorderlessButton}" Width="16" IsEnabled="{Binding FirstPageEnabled}" ToolTip="ALT+Home" Click="FirstPage_OnClick">|&lt;</Button>
                    <Button Style="{DynamicResource BorderlessButton}" Width="24" IsEnabled="{Binding PrevPageEnabled}" ToolTip="ALT+PageUp" Click="PrevPage_OnClick">&lt;&lt;</Button>
                    <TextBox  
                        MinWidth="30"
                             VerticalAlignment="Stretch" 
                             HorizontalAlignment="Stretch" 
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center"
                             KeyDown="Page_OnKeyDown"
                             Text="{Binding Page, UpdateSourceTrigger=PropertyChanged}"
                    ></TextBox>
                    <Label
                           VerticalAlignment="Stretch" 
                           HorizontalAlignment="Stretch" 
                           VerticalContentAlignment="Center"
                           HorizontalContentAlignment="Center"
                           Content="of"
                    ></Label>
                    <Label   
                            MinWidth="30"
                           VerticalAlignment="Stretch" 
                           HorizontalAlignment="Stretch" 
                           VerticalContentAlignment="Center"
                           HorizontalContentAlignment="Center"
                           Content="{Binding Pages}"
                    ></Label>
                    <Button Style="{DynamicResource BorderlessButton}" Width="24" IsEnabled="{Binding NextPageEnabled}" ToolTip="ALT+PageDown" Click="NextPage_OnClick">&gt;&gt;</Button>
                    <Button Style="{DynamicResource BorderlessButton}" Width="16" IsEnabled="{Binding LastPageEnabled}" ToolTip="ALT+End" Click="LastPage_OnClick">&gt;|</Button>
                </StackPanel>
            </Grid>
        </Grid>

        <GridSplitter Grid.Row="0" Grid.Column="1"
                      
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      ShowsPreview="True"
                      Width="5"
        />

        <Grid x:Name="PreviewGrid" DataContext="{Binding CurrentImage}" Grid.Row="0" Grid.Column="2" >
            <!--<Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto" />
                <RowDefinition/>
            </Grid.RowDefinitions>-->
            <Grid  Style="{StaticResource Preview}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"/>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="32"/>
                </Grid.RowDefinitions>
                <Image Grid.Row="0"  Grid.RowSpan="3" 
                       Source="{Binding Image}" 
                       HorizontalAlignment="Stretch" 
                       VerticalAlignment="Stretch" >
                    <Image.Visibility>
                        <MultiBinding Converter="{StaticResource invBoolToVisMulti}" ConverterParameter="20">
                            <Binding Path="NSFW"></Binding>
                            <Binding ElementName="MainGrid" Path="DataContext.NSFWBlur" ></Binding>
                        </MultiBinding>
                    </Image.Visibility>
                    <Image.Effect>
                        <BlurEffect  KernelType="Gaussian">
                            <BlurEffect.Radius>
                                <MultiBinding Converter="{StaticResource boolToBlurMulti}" ConverterParameter="20">
                                    <Binding Path="NSFW"></Binding>
                                    <Binding ElementName="MainGrid" Path="DataContext.NSFWBlur" ></Binding>
                                </MultiBinding>
                            </BlurEffect.Radius>
                        </BlurEffect>
                    </Image.Effect>
                </Image>
                <Grid Grid.Row="0" RowSpan="3" Background="Transparent">
                    <fa:ImageAwesome VerticalAlignment="Center" Height="64" Width="64" Icon="EyeSlash" Foreground="{DynamicResource ForegroundBrush}">
                        <fa:ImageAwesome.Visibility>
                            <MultiBinding Converter="{StaticResource boolToVisMulti}" ConverterParameter="20">
                                <Binding Path="NSFW"></Binding>
                                <Binding ElementName="MainGrid" Path="DataContext.NSFWBlur" ></Binding>
                            </MultiBinding>
                        </fa:ImageAwesome.Visibility>
                    </fa:ImageAwesome>
                </Grid>
                <Grid  Grid.Row="0" RowSpan="3"  Margin="10" Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <Grid Background="Transparent">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="32"/>
                        </Grid.RowDefinitions>
                        <Grid 
                            classes:VisibilityAnimation.AnimationType="Fade" 
                            Visibility="{Binding IsParametersVisible, Converter={StaticResource boolToVis}}" Grid.Row="0" Margin="10" Background="Transparent" VerticalAlignment="Bottom">
                            <Grid Margin="-20" Background="Transparent"></Grid>
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel Background="#5F000000">

                                    <Grid  Background="Transparent" >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="32"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Foreground="White"  Background="#00000000" FontSize="14" BorderThickness="0"
                                                 IsReadOnly="True"
                                                 Padding="5" TextWrapping="WrapWithOverflow" Text="{Binding Prompt}"></TextBox>
                                        <Button Style="{DynamicResource BorderlessButton}"  Grid.Column="1" Command="{Binding CopyPromptCommand}" >
                                            <Image Source="{DynamicResource CopyIcon}" Height="16" Width="16" ToolTip="Copy to clipboard"></Image>
                                        </Button>
                                    </Grid>

                                    <Grid Background="Transparent" >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="32"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Foreground="White"  Background="#00000000" FontSize="14" BorderThickness="0"
                                                 IsReadOnly="True"
                                                 Padding="5" TextWrapping="WrapWithOverflow" Text="{Binding NegativePrompt}"></TextBox>
                                        <Button Style="{DynamicResource BorderlessButton}" Grid.Column="1" Command="{Binding CopyNegativePromptCommand}" >
                                            <Image Source="{DynamicResource CopyIcon}" Height="16" Width="16" ToolTip="Copy to clipboard"></Image>
                                        </Button>
                                    </Grid>

                                    <Grid Background="Transparent" >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="32"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Foreground="White"  Background="#00000000" FontSize="14" BorderThickness="0"
                                                 IsReadOnly="True"
                                                 Padding="5" TextWrapping="WrapWithOverflow" Text="{Binding OtherParameters}"></TextBox>
                                        <!--<Button Style="{DynamicResource BorderlessButton}" Grid.Column="1" Command="{Binding CopyOthersCommand}" >
                                            <Image Source="{DynamicResource CopyIcon}" Height="16" Width="16" ToolTip="Copy to clipboard"></Image>
                                        </Button>-->
                                        <Grid Grid.Column="1"></Grid>
                                    </Grid>


                                    <Grid  Background="Transparent" >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="32"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Foreground="White" FontSize="14" BorderThickness="0"
                                             IsReadOnly="True" Padding="5" Background="Transparent" TextWrapping="WrapWithOverflow" Text="{Binding ModelName}"></TextBox>
                                        <Grid Grid.Column="1"></Grid>
                                    </Grid>

                                    <Grid  Background="Transparent" >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="32"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Foreground="White"  Background="#00000000" FontSize="14" BorderThickness="0"
                                                 IsReadOnly="True"
                                                 Padding="5" TextWrapping="WrapWithOverflow" Text="{Binding Path}"></TextBox>
                                        <Button Style="{DynamicResource BorderlessButton}"  Grid.Column="1" Command="{Binding CopyPathCommand}" >
                                            <Image Source="{DynamicResource CopyIcon}" Height="16" Width="16" ToolTip="Copy to clipboard"></Image>
                                        </Button>
                                    </Grid>

                                    <Grid  Background="Transparent" >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="32"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Foreground="White" FontSize="14" BorderThickness="0"
                                             IsReadOnly="True" Padding="5" Background="Transparent" TextWrapping="WrapWithOverflow" Text="{Binding Date}"></TextBox>
                                        <Grid Grid.Column="1"></Grid>
                                    </Grid>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>

                        <Grid  Grid.Row="1" Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>

                            <Button Background="Transparent" Style="{DynamicResource BorderlessButton}" Grid.Column="1" Command="{Binding CopyParametersCommand}">
                                <fa:ImageAwesome ToolTip="Copy Parameters" Icon="FileText" Width="20"  Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <fa:ImageAwesome.Effect>
                                        <DropShadowEffect BlurRadius="4" Opacity="0.8" ShadowDepth="3" Direction="-90"></DropShadowEffect>
                                    </fa:ImageAwesome.Effect>
                                </fa:ImageAwesome>
                            </Button>

                            <Button Background="Transparent" Style="{DynamicResource BorderlessButton}" Grid.Column="2" Command="{Binding ShowInExplorerCommand}">
                                <fa:ImageAwesome ToolTip="Show in Explorer" Icon="ExternalLink" Width="24"  Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <fa:ImageAwesome.Effect>
                                        <DropShadowEffect BlurRadius="4" Opacity="0.8" ShadowDepth="3" Direction="-90"></DropShadowEffect>
                                    </fa:ImageAwesome.Effect>
                                </fa:ImageAwesome>
                            </Button>

                            <Button Style="{DynamicResource BorderlessButton}" Grid.Column="3" Background="Transparent" BorderBrush="Transparent" BorderThickness="0" DataContext="{Binding DataContext, RelativeSource={RelativeSource FindAncestor, AncestorType=Page, AncestorLevel=1} }" Command="{Binding ToggleParameters}">
                                <fa:ImageAwesome ToolTip="Show info (I)" Icon="Eye" Width="24"  Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <fa:ImageAwesome.Effect>
                                        <DropShadowEffect BlurRadius="4" Opacity="0.8" ShadowDepth="3" Direction="-90"></DropShadowEffect>
                                    </fa:ImageAwesome.Effect>
                                </fa:ImageAwesome>
                            </Button>

                        </Grid>
                        <StackPanel Orientation="Horizontal" Grid.Row="2" >
                            <Image Height="32" Width="32" Source="../Icons/blue-heart-32.png" Visibility="{Binding Path=Favorite, Converter={StaticResource boolToVisCol}}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                            <Image Height="32" Width="32" Source="../Icons/star-32.png" Visibility="{Binding Path=Rating, Converter={StaticResource intToVis}, ConverterParameter=1}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                            <Image Height="32" Width="32" Source="../Icons/star-32.png" Visibility="{Binding Path=Rating, Converter={StaticResource intToVis}, ConverterParameter=2}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                            <Image Height="32" Width="32" Source="../Icons/star-32.png" Visibility="{Binding Path=Rating, Converter={StaticResource intToVis}, ConverterParameter=3}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                            <Image Height="32" Width="32" Source="../Icons/star-32.png" Visibility="{Binding Path=Rating, Converter={StaticResource intToVis}, ConverterParameter=4}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                            <Image Height="32" Width="32" Source="../Icons/star-32.png" Visibility="{Binding Path=Rating, Converter={StaticResource intToVis}, ConverterParameter=5}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                            <Image Height="32" Width="32" Source="../Icons/star-32.png" Visibility="{Binding Path=Rating, Converter={StaticResource intToVis}, ConverterParameter=6}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                            <Image Height="32" Width="32" Source="../Icons/star-32.png" Visibility="{Binding Path=Rating, Converter={StaticResource intToVis}, ConverterParameter=7}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                            <Image Height="32" Width="32" Source="../Icons/star-32.png" Visibility="{Binding Path=Rating, Converter={StaticResource intToVis}, ConverterParameter=8}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                            <Image Height="32" Width="32" Source="../Icons/star-32.png" Visibility="{Binding Path=Rating, Converter={StaticResource intToVis}, ConverterParameter=9}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                            <Image Height="32" Width="32" Source="../Icons/star-32.png" Visibility="{Binding Path=Rating, Converter={StaticResource intToVis}, ConverterParameter=10}">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="5" Opacity="0.8"  ShadowDepth="6" Direction="-90" RenderingBias="Quality" />
                                </Image.Effect>
                            </Image>

                        </StackPanel>
                    </Grid>
                </Grid>

            </Grid>

            <!--<GridSplitter Grid.Row="1"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Center"
                          ShowsPreview="True"
                          Height="5"
            />
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="26"/>
                </Grid.RowDefinitions>
                <ScrollViewer  Margin="2" Padding="10" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="32"/>
                                <ColumnDefinition Width="32"/>
                            </Grid.ColumnDefinitions>
                            <Label FontWeight="SemiBold">Path</Label>
                            <Button x:Name="TempButton" Style="{DynamicResource BorderlessButton}" Grid.Column="1" Command="{Binding ShowInExplorerCommand}">
                                <Image Source="{DynamicResource LinkIcon}" Height="16" Width="16" ToolTip="Show in Explorer"></Image>
                            </Button>
                            <Button Style="{DynamicResource BorderlessButton}" Grid.Column="2"  Command="{Binding CopyPathCommand}">
                                <Image Source="{DynamicResource CopyIcon}" Height="16" Width="16" ToolTip="Copy to clipboard"></Image>
                            </Button>
                        </Grid>
                        <Border BorderThickness="5" >
                            <TextBox Padding="5" BorderBrush="Transparent" Background="{DynamicResource ControlBackgroundBrush}" IsReadOnly="True" Text="{Binding Path=Path}" TextWrapping="Wrap" MaxHeight="100" VerticalScrollBarVisibility="Auto"></TextBox>
                        </Border>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="32"/>
                            </Grid.ColumnDefinitions>
                            <Label FontWeight="SemiBold">Prompt</Label>
                            <Button Grid.Column="1"  Command="{Binding CopyPromptCommand}" Background="Transparent" BorderBrush="Transparent">
                                <Image Source="{DynamicResource CopyIcon}" Height="16" Width="16" ToolTip="Copy to clipboard"></Image>
                            </Button>
                        </Grid>
                        <Border BorderThickness="5" >
                            <TextBox Padding="5" BorderBrush="Transparent" Background="{DynamicResource ControlBackgroundBrush}" IsReadOnly="True" Text="{Binding Prompt}" TextWrapping="Wrap" MaxHeight="100" VerticalScrollBarVisibility="Auto"></TextBox>
                        </Border>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="32"/>
                            </Grid.ColumnDefinitions>
                            <Label FontWeight="SemiBold">Negative Prompt</Label>
                            <Button Grid.Column="1" Command="{Binding CopyNegativePromptCommand}" Background="Transparent" BorderBrush="Transparent">
                                <Image Source="{DynamicResource CopyIcon}" Height="16" Width="16" ToolTip="Copy to clipboard"></Image>
                            </Button>
                        </Grid>
                        <Border BorderThickness="5" >
                            <TextBox Padding="5" BorderBrush="Transparent" Background="{DynamicResource ControlBackgroundBrush}" IsReadOnly="True" Text="{Binding NegativePrompt}" TextWrapping="Wrap" MaxHeight="100" VerticalScrollBarVisibility="Auto"></TextBox>
                        </Border>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="32"/>
                            </Grid.ColumnDefinitions>
                            <Label FontWeight="SemiBold">Others</Label>
                            -->
            <!--<Button Grid.Column="1" Background="Transparent" BorderBrush="Transparent">
                                <Image Source="../Icons/copy-32.png" Height="16" Width="16"></Image>
                            </Button>-->
            <!--
                        </Grid>

                        <Border BorderThickness="5" >
                            <TextBox Padding="5" BorderBrush="Transparent" Background="{DynamicResource ControlBackgroundBrush}" IsReadOnly="True" Text="{Binding OtherParameters}" TextWrapping="Wrap" MaxHeight="100" VerticalScrollBarVisibility="Auto"></TextBox>
                        </Border>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="32"/>
                            </Grid.ColumnDefinitions>
                            <Label FontWeight="SemiBold">Model Name</Label>
                            -->
            <!--<Button Grid.Column="1" Background="Transparent" BorderBrush="Transparent">
                                <Image Source="../Icons/copy-32.png" Height="16" Width="16"></Image>
                            </Button>-->
            <!--
                        </Grid>

                        <Border BorderThickness="5" >
                            <TextBox Padding="5" BorderBrush="Transparent" Background="{DynamicResource ControlBackgroundBrush}" IsReadOnly="True" Text="{Binding ModelName}" TextWrapping="Wrap" MaxHeight="100" VerticalScrollBarVisibility="Auto"></TextBox>
                        </Border>


                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="32"/>
                            </Grid.ColumnDefinitions>
                            <Label FontWeight="SemiBold">Date</Label>
                            -->
            <!--<Button Grid.Column="1" Background="Transparent" BorderBrush="Transparent">
                                <Image Source="../Icons/copy-32.png" Height="16" Width="16"></Image>
                            </Button>-->
            <!--
                        </Grid>

                        <Border BorderThickness="5" >
                            <TextBox Padding="5" BorderBrush="Transparent" Background="{DynamicResource ControlBackgroundBrush}" IsReadOnly="True" Text="{Binding Date}" TextWrapping="Wrap" MaxHeight="100" VerticalScrollBarVisibility="Auto"></TextBox>
                        </Border>


                    </StackPanel>
                </ScrollViewer>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Command="{Binding ShowInThumbnails}">Show in Thumbnails</Button>
                    <Button Grid.Column="1" Command="{Binding CopyParametersCommand}">Copy Parameters</Button>
                </Grid>
            </Grid>-->
        </Grid>
    </Grid>
</Page>
 